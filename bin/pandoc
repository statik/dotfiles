#!/usr/bin/env bash

if [[ $(hostname) == "havendev" ]]; then
  pandoc_cmd=${HOME}/.linuxbrew/bin/pandoc
else
  pandoc_cmd=/opt/homebrew/bin/pandoc
fi

#echo DEBUG pandoc $* >> pandoc-debug.log

exec ${pandoc_cmd} "$@"
